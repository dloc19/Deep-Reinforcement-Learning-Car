# Thư mục này được bao phủ bởi root .gitignore ở cấp dự án.
# Chỉ thêm rule đặc thù cho thư mục con này nếu cần thiết.

# Dataset đầu ra của bộ thu thập dữ liệu CARLA
dataset/

# Python cache cục bộ (phòng khi git chạy từ thư mục này)
__pycache__/
*.pyc
